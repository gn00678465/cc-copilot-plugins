背景文件（本機 untracked）：
  .artifacts/code-review-loop-bug-report.md  — 完整原始分析

對應原始議題：
  - P1 (High)  — computeNextRange 連續 stash 產生空 diff
  - P4 (Low)   — no-diff 提示訊息不夠明確

驗證情境（unit-test 通過）：
  1. 同 tree 不同 SHA：用 git commit-tree 偽造一個指向相同 tree 的 commit
     當作 prevRef，gitStashCreate 產生新 SHA → gate 命中 → no-diff
  2. 不同 tree（控制案）：working tree 真的有新變動 → gate 不觸發 →
     正常回 base/head 範圍
  3. no-diff stderr 訊息：在多個情境下都印出含 commit 指引的多行說明

接下個 commit（600ca6e）：實作此 commit 中 gitTreeHash 時用了
`git rev-parse <ref>^{tree}`，但在 Windows cmd 環境下 `^` 被吞，
gate 形同關閉。已於 600ca6e 修復。本 commit 的設計與意圖正確，
僅實作細節在 Windows 平台需配合下個 commit 一起 review。
